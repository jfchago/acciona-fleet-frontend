# Frontend Next.js API First

Cliente Next.js con TypeScript generado desde `../../.metacontext/deliverables/contracts/OpenApi.yaml`.

```powershell
npm install
npm run generate
npm run typecheck
npm run dev
```

Configura `NEXT_PUBLIC_API_BASE_URL` para cambiar la URL del backend.
